import React from 'react';
import {GRAPH_ROOT_CLASS_NAME} from '../graph/constants';

export default function HierarchyGraphView() {
    return (
        <div className={GRAPH_ROOT_CLASS_NAME} />
    );
}